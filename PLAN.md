# 项目计划 — visualizeLOB

## 摘要
简要说明：以Python和plotly为工具，构建一个订单簿（Limit Order Book, LOB）动态可视化工具，用于展示买卖挂单随时间逐帧变化的回放视图。

**目标与范围**
- 数据输入：
    - orderbook.parquet: 订单簿的每次变化的结果（买卖10档以内的价格和剩余挂单量）
    - triggerInfo.parquet: 引起订单簿发生变化的那笔行情（order或cancel，其中order可能引起交易也可能只是挂单）
- 可视化当前买卖10档的深度、导致当前订单簿状态的逐笔行情（挂单、撤单、成交）、微观结构动画
- 提供交互（缩放、过滤、时间控制）

---

## 里程碑（Milestones）
- M1 — toy数据的生成(orderbook.parquet和triggerInfo.parquet)
- M2 — 数据输入接口（配置orderbook.parquet和triggerInfo.parquet的地址，并框定日期、票号、起始时间(或订单簿的某个index)、结束时间）
- M3 — 基础可视化（深度图、订单流）
- M4 — 交互与性能优化
- M5 — 文档与示例数据，发布

---

## 任务清单（Tasks）
- [ ] 定义数据规范（CSV/JSON、字段含义）
- [ ] 搭建项目结构（前端/后端/工具脚本）
- [ ] 实现数据加载模块（CSV/WS）
- [ ] 实现基本深度可视化（buy/sell depth）
- [ ] 实现逐笔成交显示
- [ ] 添加回放时间控制（播放/暂停/速率）
- [ ] 性能基准与优化（大数据量下渲染）
- [ ] 写示例数据与使用说明

每项任务应包含验收标准与估时（在任务被拆分时补充）。

---

## 依赖与风险（Dependencies & Risks）
- 风险：实时数据源不稳定 → 缓冲/回放模式作为降级方案
- 依赖：可视化库（例如 D3/Canvas/WebGL）→ 评估后选定
- 依赖：浏览器性能与内存限制 → 需做数据抽样/聚合策略

---

## 验收标准（Acceptance Criteria）
- 能加载并播放一份 1-hour 的示例订单簿数据，且无明显卡顿
- 能正确显示买卖深度与逐笔成交，且时间轴可控
- 提供至少一份 README 使用示例与可视化截图

---

## 时间线（建议）
- 周 1: 里程碑 M1（初始化）
- 周 2–3: M2（数据接口）
- 周 4–6: M3（基础可视化）
- 周 7–8: M4（交互 + 优化）
- 周 9: M5（文档与发布）

---

## 备注与后续
把你的优先级、可用数据样本和你希望的可视化样式（例如热力图、阶梯图、簇状条形）告诉我，我会把任务拆成更细的可执行子任务并更新本 `PLAN.md` 与 TODO 列表。
